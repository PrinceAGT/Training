Questions on Table: -  

1. Create a table called Student with the following fields: 

Student No. (Code [20]) – Primary Key 

Student Name (Text [100]) 

Age (Integer) 

  

2. In the Student table: 

Add a field Status (Boolean). 

Set its default value to TRUE. 

  

3.In the Student table, write logic so that: 

When a new student record is created, 

The Status field automatically becomes TRUE. 

Explore more about Table Triggers (Each) 

  

4.In the Student table: 

Add a field Date of Birth (Date). 

Add a field Age (Integer). 

In the OnValidate trigger of Date of Birth, 

calculate Age automatically. 

  

5. Create another table called Course with: 

Course Code (Code [20]) 

Course Name (Text [100]) 

  

Now in Student table: 

Add field Course Code 

Use TableRelation to link it with Course table. 

  

6.Create a table called Enrollment: 

Student No. 

Course Code 

  

Now in Student table: 

Create a FlowField called Total Courses 

It should count how many enrollments the student has. 

  

7.In Enrollment table, add: 

Enrollment Date (Date) 

  

Now in Student table: 

Create a Date Filter (FlowFilter field). 

Modify Total Courses FlowField so that it counts enrollments only within the selected date filter. 